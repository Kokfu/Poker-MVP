import type {Analysis} from '../types';
export function AnalysisResults({data}:{data:Analysis}){const pct=(n:number)=>(n*100).toFixed(1)+'%';return <section className="results"><h2>Analysis</h2><div className="metrics"><p><small>Street</small>{data.street}</p><p><small>Hand</small>{data.hand_label}</p><p><small>Win / Tie / Loss</small>{pct(data.win_rate)} / {pct(data.tie_rate)} / {pct(data.loss_rate)}</p><p><small>Equity</small>{pct(data.equity)}</p><p><small>Required equity</small>{pct(data.required_equity)}</p><p><small>Final pot if call</small>{data.final_pot_if_call}</p><p><small>Method</small>{data.calculation_method.replace('_',' ')} ({data.iterations.toLocaleString()})</p></div><h3>{data.recommendation}</h3><p>{data.explanation}</p><p><strong>Draws:</strong> {data.draws.length?data.draws.map(d=>d.type.replaceAll('_',' ')).join(', '):'None'}</p><p className="muted">{data.disclaimer}</p></section>}
