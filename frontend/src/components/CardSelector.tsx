const ranks=['A','K','Q','J','T','9','8','7','6','5','4','3','2'], suits=['s','h','d','c'];
export function CardSelector({cards,setCards}:{cards:(string|null)[];setCards:(v:(string|null)[])=>void}){const [active,setActive]=useState(0);const labels=['Hero Card 1','Hero Card 2','Board Card 1','Board Card 2','Board Card 3','Board Card 4','Board Card 5'];return <section><h2>Cards</h2><div className="slots">{labels.map((label,i)=><button className={'slot '+(active===i?'active':'')} onClick={()=>setActive(i)} key={label}>{label}<b>{cards[i]||'—'}</b>{cards[i]&&<span onClick={e=>{e.stopPropagation();const next=[...cards];next[i]=null;setCards(next)}}>×</span>}</button>)}</div><div className="deck">{ranks.map(r=>suits.map(s=>{const c=r+s;return <button disabled={cards.includes(c)} className={s==='h'||s==='d'?'red':''} onClick={()=>{const next=[...cards];next[active]=c;setCards(next);if(active<6)setActive(active+1)}} key={c}>{r}{s==='s'?'♠':s==='h'?'♥':s==='d'?'♦':'♣'}</button>}))}</div><button className="secondary" onClick={()=>{setCards(Array(7).fill(null));setActive(0)}}>Reset all</button></section>}
import {useState} from 'react';
